
import type { Metadata } from "next";
import "./d98.css";

export const metadata: Metadata = {
  title: "D98.AI — Premier Intelligence System",
  description: "D98.AI - Next-Level AI Intelligence. Premier Artificial Intelligence Engine. Beyond Human Limits. The Future Is Now.",
};

export default function D98Layout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
